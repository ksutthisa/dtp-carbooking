name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Bun / Node
        uses: oven-sh/setup-bun@v1 # หรือถ้าใช้ npm ให้เปลี่ยนเป็น setup-node ตามความเหมาะสม

      - name: Install Dependencies & Build
        run: |
          bun install
          bun run build

      - name: Deploy
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
